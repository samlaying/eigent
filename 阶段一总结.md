# 阶段一总结：壳层重构

## 新增文件

### 1. `src/store/navigationStore.ts`
管理全局导航状态：
- `activeView: 'home' | 'history' | 'settings'` — 当前活跃视图
- `listPanelOpen: boolean` — 左侧会话列表面板显隐
- `artifactPanelOpen: boolean` — 右侧 Artifact 面板显隐
- 配套 setter / toggle 方法

### 2. `src/components/NavBar/index.tsx`
左侧竖排导航栏，替代原 TopBar 中的水平导航链接：
- 三个图标按钮：Home、History、Settings
- 使用 `navigationStore.activeView` 控制高亮状态
- 点击时调用 `navigate()` + `setActiveView()`

### 3. `src/components/ListPanel/index.tsx`
可折叠的左侧会话列表面板：
- 读取 `projectStore.projects` 渲染项目列表
- 每个项目下显示子 chat stores 及状态指示符（运行中/已完成/待开始）
- "New Chat" 按钮创建新项目
- 点击项目切换 `activeProjectId`
- 关闭按钮折叠面板

### 4. `src/components/ArtifactPanel/index.tsx`
右侧 Artifact 占位面板（默认折叠）：
- 标题栏 + 关闭按钮
- 占位文本提示（Phase E 实现内容检测与渲染）

## 修改文件

### 1. `src/components/Layout/index.tsx`
- 在 Main Content 区域左侧插入 `<NavBar />`
- NavBar 仅在 `shouldShowMainContent` 为 true 时渲染（安装/引导页隐藏）
- 导入新增 NavBar 组件

### 2. `src/components/TopBar/index.tsx`
- 移除 `House` 导航按钮（移到 NavBar）
- 移除 `Settings` 导航按钮（移到 NavBar）
- 移除对应 import：`House`, `Settings`
- 保留：窗口控制、应用图标、新建项目按钮、项目标题、右侧操作区（结束/分享/导出/推荐）

### 3. `src/pages/Home.tsx`
从 2 栏（ChatBox | Workspace）改为 4 栏布局壳：

```
[Nav] ← Layout 层 | [List] | [Main: ChatBox + Workspace] | [Artifact]
```

具体改造：
- 外层新增 `ResizablePanelGroup`，包含 List / Main / Artifact 三个 panel
- List Panel（默认 18%，最小 12%，最大 30%，可折叠）
- Main Panel（弹性填充，保留原有 ChatBox + workspace 全部逻辑）
- Artifact Panel（默认 22%，最小 12%，最大 40%，默认折叠）
- 嵌套保留原有 `ResizablePanelGroup(ChatBox | Workspace | ChatBox)` 结构
- 导入新增组件：`ListPanel`, `ArtifactPanel`, `useNavigationStore`
- 所有工作区功能（workforce/inbox/triggers 标签、webview capture、file upload、dialog）完整保留

## 未改动

- workspace 内容渲染逻辑（`renderWorkspaceContent`）
- ChatBox 左右布局切换
- webview 轮询截图机制
- 文件上传流程
- 所有 Dialog（AddWorker、TriggerDialog、EndNotice）
- BottomBar 行为
- 路由结构
- 后端代码
- Electron 层

## 验证

- TypeScript type check 通过（新增/修改文件无类型错误）
- Vite build 成功（361 modules transformed）
- Electron 窗口正常启动
- 后端正常启动
